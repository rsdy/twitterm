#ifndef __MAIN_H
#define __MAIN_H
#include <stdlib.h>
#include <string.h>

/** @file */

/** Duplicates the string in parameter
	* @param str the string to duplicate
	* @return an allocated string with the same content as str*/
char *mystrdup(char *str);

#endif
